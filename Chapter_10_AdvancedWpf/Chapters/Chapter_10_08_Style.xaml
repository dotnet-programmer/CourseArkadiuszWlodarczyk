<Window x:Class="Chapter_10_AdvancedWpf.Chapters.Chapter_10_08_Style"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="Chapter_10_08_Style"
		Height="500"
		Width="600"
		WindowStartupLocation="CenterScreen">

	<StackPanel>
		<ToggleButton x:Name="BtnSurvey"
					  Content="Pokaż ankietę"
					  IsChecked="{Binding ElementName=Survey, Path=IsOpen}" />

		<Popup x:Name="Survey"
			   AllowsTransparency="True"
			   PlacementTarget="{Binding ElementName=BtnSurvey}">

			<StackPanel>
				
				<StackPanel.Resources>
					<Style TargetType="{x:Type GroupBox}"
						   x:Key="HeaderLook">
						
						<Setter Property="BorderThickness"
								Value="2" />
						
						<Setter Property="Foreground"
								Value="Crimson" />
						
						<Setter Property="HeaderTemplate">
							<Setter.Value>
								<DataTemplate>
									<!--Binding w Template zachowuje się inaczej, nie odnosi się do DataContext,
									tylko do wartości która akurat jest w miejscu dla którego tworzony jest szablon
									Czyli tutaj Text="{Binding}" oznacza bindowanie do tekstu w headerze GroupBoxa-->
									<TextBlock Text="{Binding}"
											   Background="LightBlue"
											   Padding="3"
											   FontWeight="Bold" />
								</DataTemplate>
							</Setter.Value>
						</Setter>
						
					</Style>
				</StackPanel.Resources>

				<GroupBox Margin="10"
						  Header="Ile grasz dziennie na komputerze?"
						  Style="{StaticResource HeaderLook}">
					<StackPanel Margin="10">
						<RadioButton Content="wcale" />
						<RadioButton Content="1-3h" />
						<RadioButton Content="3-6h" />
						<RadioButton Content="24/h ;)" />
					</StackPanel>
				</GroupBox>

				<GroupBox Margin="10"
						  Header="Ile dziennie śpisz?"
						  Style="{StaticResource HeaderLook}">
					<StackPanel Margin="10">
						<RadioButton Content="wcale" />
						<RadioButton Content="1-3h" />
						<RadioButton Content="3-6h" />
						<RadioButton x:Name="RbMore">
							<WrapPanel x:Name="PopupPanel">
								<Label Content="więcej:" />
								<Popup IsOpen="{Binding ElementName=RbMore, Path=IsChecked}"
									   PlacementTarget="{Binding ElementName=PopupPanel}"
									   HorizontalOffset="50"
									   Placement="Right"
									   PopupAnimation="Scroll"
									   AllowsTransparency="True">
									<TextBox MinWidth="50" />
								</Popup>
							</WrapPanel>
						</RadioButton>
					</StackPanel>
				</GroupBox>
			</StackPanel>
		</Popup>
	</StackPanel>
</Window>