<Window x:Class="Chapter_10_AdvancedWpf.Chapters.Chapter_10_22_Listview"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="Chapter_10_22_Listview"
		Height="400"
		Width="600"
		WindowStartupLocation="CenterScreen">

	<StackPanel>
		<ListView x:Name="ListOfNames"
				  SelectionMode="Extended">
			<ListView.View>
				<GridView AllowsColumnReorder="False">
					<GridView.ColumnHeaderTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding}"
									   FontWeight="Bold" />
						</DataTemplate>
					</GridView.ColumnHeaderTemplate>

					<GridView.ColumnHeaderContainerStyle>
						<Style TargetType="GridViewColumnHeader">
							<Setter Property="FontSize"
									Value="15" />
							<Setter Property="Padding"
									Value="5" />
							<Setter Property="HorizontalContentAlignment"
									Value="Center" />
						</Style>
					</GridView.ColumnHeaderContainerStyle>

					<GridView.Columns>
						<GridViewColumn Header="Name"
										Width="100"
										DisplayMemberBinding="{Binding Path=Name}" />
						<GridViewColumn Header="Age"
										Width="50"
										DisplayMemberBinding="{Binding Path=Age}" />
						<GridViewColumn Header="Email"
										Width="200">
							<GridViewColumn.CellTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding Path=Email}"
											   Foreground="Blue"
											   TextDecorations="Underline" />
								</DataTemplate>
							</GridViewColumn.CellTemplate>
						</GridViewColumn>
					</GridView.Columns>
				</GridView>
			</ListView.View>

			<ListView.ItemContainerStyle>
				<Style TargetType="{x:Type ListBoxItem}">
					<Setter Property="Padding"
							Value="5" />
					<Style.Triggers>
						<MultiTrigger>
							<MultiTrigger.Conditions>
								<Condition Property="IsSelected"
										   Value="True" />
								<Condition Property="IsFocused"
										   Value="True" />
							</MultiTrigger.Conditions>
							<Setter Property="FontSize"
									Value="15" />
							<Setter Property="FontWeight"
									Value="Bold" />
						</MultiTrigger>
					</Style.Triggers>
				</Style>
			</ListView.ItemContainerStyle>
		</ListView>

		<Button Click="Button_Click_1"
				Content="Usuń zaznaczony element" />

		<WrapPanel>
			<Label Content="Imię:" />
			<TextBox x:Name="NameToAdd"
					 Width="100" />
		</WrapPanel>
		<WrapPanel>
			<Label Content="Wiek:" />
			<TextBox x:Name="AgeToAdd"
					 Width="100" />
		</WrapPanel>
		<WrapPanel>
			<Label Content="Email:" />
			<TextBox x:Name="EmailToAdd"
					 Width="100" />
		</WrapPanel>
		<Button Click="Button_Click"
				Content="Dodaj do listy" />
	</StackPanel>
</Window>