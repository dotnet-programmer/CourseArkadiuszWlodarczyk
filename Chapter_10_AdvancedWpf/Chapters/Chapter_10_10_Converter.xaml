<Window x:Class="Chapter_10_AdvancedWpf.Chapters.Chapter_10_10_Converter"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:helpers="clr-namespace:Chapter_10_AdvancedWpf.Helpers"
		Title="Chapter_10_10_Converter"
		Height="500"
		Width="600"
		WindowStartupLocation="CenterScreen">

	<Window.Resources>
		<helpers:BoolVisibilityConverter x:Key="BoolVisibilityConverter" />
	</Window.Resources>

	<StackPanel>
		<ToggleButton x:Name="BtnSurvey">
			<ToggleButton.Style>
				<Style TargetType="{x:Type ToggleButton}">
					<Setter Property="FontSize"
							Value="25" />
					<Setter Property="Content"
							Value="Pokaż ankietę" />
					<Style.Triggers>
						<Trigger Property="IsChecked"
								 Value="True">
							<Setter Property="Content"
									Value="Ukryj ankietę" />
							<Setter Property="FontWeight"
									Value="Bold" />
						</Trigger>
					</Style.Triggers>
				</Style>
			</ToggleButton.Style>
		</ToggleButton>

		<StackPanel Visibility="{Binding ElementName=BtnSurvey, Path=IsChecked, Converter={StaticResource BoolVisibilityConverter}}">
			<StackPanel.Resources>
				<Style TargetType="{x:Type GroupBox}"
					   x:Key="HeaderLook">
					<Setter Property="Foreground"
							Value="Crimson" />
					<Setter Property="HeaderTemplate">
						<Setter.Value>
							<DataTemplate>
								<TextBlock Text="{Binding}"
										   Background="LightBlue"
										   Padding="3"
										   FontWeight="Bold" />
							</DataTemplate>
						</Setter.Value>
					</Setter>
					<Setter Property="BorderThickness"
							Value="2" />
				</Style>
			</StackPanel.Resources>

			<GroupBox Margin="10"
					  Header="Ile grasz dziennie na komputerze?"
					  Style="{StaticResource HeaderLook}">
				<StackPanel Margin="10">
					<RadioButton Content="wcale" />
					<RadioButton Content="1-3h" />
					<RadioButton Content="3-6h" />
					<RadioButton Content="24/h ;)" />
				</StackPanel>
			</GroupBox>

			<GroupBox Margin="10"
					  Header="Ile dziennie śpisz?"
					  Style="{StaticResource HeaderLook}">
				<StackPanel Margin="10">
					<RadioButton Content="wcale" />
					<RadioButton Content="1-3h" />
					<RadioButton Content="3-6h" />
					<RadioButton x:Name="RbMore">
						<WrapPanel x:Name="PopupPanel">
							<Label Content="więcej:" />
							<Popup IsOpen="{Binding ElementName=RbMore, Path=IsChecked}"
								   PlacementTarget="{Binding ElementName=PopupPanel}"
								   HorizontalOffset="50"
								   Placement="Right"
								   PopupAnimation="Scroll"
								   AllowsTransparency="True">
								<TextBox MinWidth="50" />
							</Popup>
						</WrapPanel>
					</RadioButton>
				</StackPanel>
			</GroupBox>
		</StackPanel>

		<Button Content="Test" />
	</StackPanel>
</Window>