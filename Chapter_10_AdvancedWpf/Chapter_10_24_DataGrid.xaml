<Window x:Class="Chapter_10_AdvancedWpf.Chapter_10_24_DataGrid"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d"
		Title="Chapter_10_24_DataGrid"
		Height="400"
		Width="600"
		WindowStartupLocation="CenterScreen">

	<StackPanel>

		<DataGrid x:Name="dataGridOfPeople"
				  SelectionMode="Extended"
				  SelectionUnit="CellOrRowHeader"
				  AlternatingRowBackground="BurlyWood"
				  AlternationCount="2"
				  FrozenColumnCount="1"
				  HeadersVisibility="All"
				  AutoGenerateColumns="False"
				  CanUserAddRows="False">

			<DataGrid.Columns>
				<DataGridTextColumn Header="Name"
									Binding="{Binding Path=Name}">

					<DataGridTextColumn.CellStyle>
						<Style TargetType="DataGridCell">
							<Setter Property="Background"
									Value="Azure" />
						</Style>
					</DataGridTextColumn.CellStyle>

					<DataGridTextColumn.HeaderStyle>
						<Style TargetType="DataGridColumnHeader">
							<Setter Property="FontWeight"
									Value="Bold" />
						</Style>
					</DataGridTextColumn.HeaderStyle>

					<DataGridTextColumn.HeaderTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding}" />
						</DataTemplate>
					</DataGridTextColumn.HeaderTemplate>
				</DataGridTextColumn>

				<DataGridTextColumn Header="Age"
									Binding="{Binding Path=Age}" />

				<DataGridTextColumn Header="Email"
									Binding="{Binding Path=Email}" />

				<DataGridCheckBoxColumn Header="Newsletter"
										Binding="{Binding Path=IsSubscribedToNewsletter}" />

				<DataGridHyperlinkColumn Header="WWW"
										 Binding="{Binding Path=Website}" />

				<DataGridComboBoxColumn Header="Sex"
										x:Name="ComboxBoxColumnSex"
										SelectedValueBinding="{Binding Path=GenderType}" />

				<DataGridTemplateColumn Header="Image">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Image Source="{Binding Path=Image}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>

			<DataGrid.RowStyle>
				<Style TargetType="DataGridRow">
					<Setter Property="Background"
							Value="AntiqueWhite" />
				</Style>
			</DataGrid.RowStyle>

			<DataGrid.RowDetailsTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Path=Description}" />
				</DataTemplate>
			</DataGrid.RowDetailsTemplate>
		</DataGrid>
	</StackPanel>
</Window>